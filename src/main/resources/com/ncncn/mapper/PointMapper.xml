<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ncncn.mapper.PointMapper">

    <insert id="accRegister">
        insert into bank_account
            value ( #{acc},
            #{userId},
            #{bnkCode},
            #{holder},
            #{isAuthed},
            now(),
            "001",
            now(),
            current_user (),
            now(),
            current_user ());
    </insert>

    <update id="accUpdate">
        update bank_account
        set acc=#{acc},
            bnk_code = #{bnkCode},
            holder = #{holder},
            auth_dt = now(),
            up_date = now(),
            is_authed = #{isAuthed},
            up_user = current_user()
        where user_id = #{userId};
    </update>

</mapper>